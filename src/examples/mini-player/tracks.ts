// Same catalog as the Music Player example — the two demos share one album,
// so the accessory here shows the same songs and artwork as the full player.
export { TRACKS, type Track } from '../music-player/tracks';
